---
layout: single
title:  "JVM, 넌 누구냐?"
permalink: /language/java/jvm
folder: "java"
categories:
  - java
tags: 
  - java
  - jvm

author_profile: true
sidebar:
  nav: "docs"

toc: true
toc_label: "목록"
toc_icon: "bars"
toc_sticky: true

date: 2021-12-25
---
## 지금까지 자바에 대한 태도
지금까지 자바라는 언어를 단순히 익히기에 급급했던 것 같다.   
그래서 동시성이라는 개념에 부딪혔을 때 이해하지 못하고 어떻게 사용하는가라는 잘못된 생각을 하게 되었다. 
처음으로 돌아가 자바의 JVM에 대해 정리를 하고자 한다.

## JVM이란?
JVM(Java Virtual Machine)은 자바 바이트 코드를 실행해주는 스택기반의 가상 머신이다. 
많이 들어봤지만 하나씩 보면 이해가 안되는 부분들이 있다.

### 바이트 코드를 실행?
예전부터 자바를 공부를 하면서 자바 코드를 바이트 코드로 컴파일 된다라는 것은 이미 알고 있었지만
  
바이트 코드를 실행한다?  
  
이 전에 바이트 코드와 바이너리 코드의 차이를 알아야 한다.  
  
바이너리 코드는 CPU가 인식할 수 있는 0과 1로 이루어져 있는 이진코드,
바이트 코드는 가상머신이 이해할수 있는 코드  
  
그럼 JVM이 바이트 코드를 실행해준다함은 JVM이 이해할수 있는 코드,   
바이트코드를 이해할 수 있는 JVM의 실행엔진이 바이트 코드를 CPU가 실행할 수 있는 바이너리 코드로 변환하는 작업하고 실행한다.
  
왜 바이너리 코드아닌 바이트 코드로 변환을 해서 JVM에 전달할까? 
  
자바가 플랫폼에 독립적이다라는 것에 있다.  
CPU에 따라 기계어의 설계가 다르고 OS마다 인식하는 것이 다르기 때문에  
어떤 기계어를 여러 환경에서 실행을 할 수 없다.   
하지만 자바는 자바 코드를 JVM가 이해할 수 있는 바이트 코드로 컴파일하고 각 환경에 JVM만 있으면    
그 OS가 해석할 수 있는 바이너리 코드로 컴파일하기 때문에 플랫폼에 독립적이게 되고  
그 바이트 코드를 실행하기 위해 필요한 것이 JVM이다.


### 스택 기반의 가상 머신
가상머신을 구현하는 방법에는 두가지가 있는데 스택기반, 레지스터 기반이 있다.  
스택 기반 VM은 연산을 할 때 스택구조 이기때문에 PUSH POP 사용하여 연산을 하고 POP을 하면 피연산자가 나오기 때문에 피연산자의 메모리 위치를 기억할 필요가 없다.  
레지스터 기반 VM은 피연산자가 CPU의 레지스터에 저장이된다. POP & PUSH 과정이 없기 때문에 명령어가
피연산자의 위치인 레지스터의 주소를 기억해야 한다. 그렇기에 스택기반의 명령어의 길이가 길다.

## JVM의 구조
<img width="1109" alt="스크린샷 2021-12-24 오후 11 42 41" src="https://user-images.githubusercontent.com/71247713/147360720-3f40e999-d0a4-48ad-bdf2-5807b6ca3bc6.png">
구조는 위와 같이 구성이 되어있고 이에 대하여 이야기할 부분이 많아서 따로 정리를 해야겠다.


## 자바 실행 과정
자바 실행 과정을 간단하게 정리하자면  
1. 자바코드(.java)는 자바 컴파일러(javac.exe)에 의해서 바이트 코드(.class)로 변환이 된다.
2. 컴파일된 바이트 코드는 클래스 로더에 의하여 JVM에 적재가 된다.
3. 그 적재된 코드는 execution engine에 의하여 바이트 코드를 해석되어 실행하게 된다.

### 인터프리터, JIT 컴파일러 방식
아까전에 바이트 코드를 JVM이 해석을 한다고 했었는데 사실  
해석하는 방식에는 인터프리터 방식, JIT 컴파일러 방식이 있다.  
바이트 코드를 실행할 때
한번 실행될 코드는 인터프리터로 실행을 하고 주기적으로 사용될 코드들은 체킹을 하고 
일정 횟수가 넘어가면 JIT로 컴파일을 한다.



## 참고 자료
<https://velog.io/@litien/JVM-%EA%B5%AC%EC%A1%B0>
<https://p-op.tistory.com/3>
<https://quasarzone.com/bbs/qf_cmr/views/31834>
<https://t0pli.tistory.com/51>
<https://www.korecmblog.com/jvm-stack-and-register/>


